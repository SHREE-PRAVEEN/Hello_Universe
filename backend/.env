# RoboVeda Backend Environment Configuration

# Server Configuration
HOST=0.0.0.0
PORT=8080

# Database Configuration
DATABASE_URL=postgres://shreepraveen:shreepraveen@localhost:5432/HELLO_UNIVERSE_DB
DB_MAX_CONNECTIONS=10
DB_MIN_CONNECTIONS=2
DB_CONNECT_TIMEOUT=10
DB_IDLE_TIMEOUT=600
DB_MAX_LIFETIME=1800

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-2026
JWT_EXPIRATION=86400

# Frontend URL (for CORS and email links)
FRONTEND_URL=http://localhost:3000

# Logging
RUST_LOG=backend=debug,actix_web=info,sqlx=warn
